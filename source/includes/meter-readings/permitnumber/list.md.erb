## List Meter Readings

> Send a get request to the meter-readings endpoint with optional query parameters:

```shell
curl "<%= config[:endpoint] %>/api/meter-readings?permit=FP-63209&year=2021&year=2022"
```

```javascript
const axios = require("axios");

const options = {
  method: "GET",
  url: "<%= config[:endpoint] %>/api/meter-readings",
  params: {
    permit: "63209-FP",
    year: ["2021", "2022"],
  },
};

axios
  .request(options)
  .then((response) => {
    console.log(response.data);
  })
  .catch((error) => {
    console.error(error);
  });
```

> Successful deletion will return the deleted records in JSON:

```json
[
  {
    "date": "2021-01",
    "permitNumber": "FP-63209"
    // etc...
  },
  {
    "date": "2021-02",
    "permitNumber": "FP-63209"
    // etc...
  },
  {
    "date": "2022-01",
    "permitNumber": "FP-63209"
    // etc...
  }
  // etc...
]
```

This endpoint retrieves meter readings for a permit number.

### HTTP Request

`GET /api/meter-readings`

### Query Parameters

| Parameter    | Type   | Restrictions | Description                                          |
| ------------ | ------ | ------------ | ---------------------------------------------------- |
| permitNumber | string | optional     | A string matching the well permit number.            |
| year         | string | optional     | A string matching the calandar year of well permits. |
| date         | string | optional     | A string formatted 'YYYY-MM'.                        |
