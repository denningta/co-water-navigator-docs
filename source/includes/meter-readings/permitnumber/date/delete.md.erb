## Delete a Meter Reading

```shell
curl "<%= config[:endpoint] %>/api/meter-readings/63209-FP/2022-06" \\
  -X DELETE
```

```javascript
const axios = require("axios");

const options = {
  method: "DELETE",
  url: "<%= config[:endpoint] %>/api/meter-readings/632090-FP/2022-06",
};

axios
  .request(options)
  .then((response) => {
    console.log(response.data);
  })
  .catch((error) => {
    console.error(error);
  });
```

> The above command returns the record deleted:

```json
{
  "date": "2022-06",
  "permitNumber": "63209-FP",
  "flowMeter": {
    "value": 200
  }
}
```

This endpoint deletes a meter reading.

### HTTP Request

`DELETE /api/meter-readings/{permitNumber}/{date}`

### URL Parameters

| Parameter    | Type   | Restrictions | Description                               |
| ------------ | ------ | ------------ | ----------------------------------------- |
| permitNumber | string | required     | A string matching the well permit number. |
| date         | string | required     | A string formatted 'YYYY-MM'.             |
