## Get an Administrative Report

```shell
curl "https://cowaternavigator/api/administrative-reports/FP-63209/2022" \
  -H "Authorization: token"
```

```javascript
const axios = require("axios");

axios
  .get("https://cowaternavigator/api/administrative-reports/FP-63209/2022")
  .then((response) => {
    // handle success
    console.log(response);
  })
  .catch((error) => {
    // handle error
    console.log(error);
  });
```

> The above command returns JSON structured like this:

```json
{
  "date": "2022-06",
  "permitNumber": "FP-63209",
  "flowMeter": {
    "value": 150
  }
  // etc...
}
```

This endpoint retrieves a specific meter reading.

### HTTP Request

`GET /api/administrative-report/{permitNumber}/{year}`

### URL Parameters

| Parameter    | Required | Description                               |
| ------------ | -------- | ----------------------------------------- |
| permitNumber | true     | A string matching the well permit number. |
| year         | true     | A string formatted 'YYYY-MM'.             |
