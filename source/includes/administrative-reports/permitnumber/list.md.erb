## List Administrative Reports

```shell
curl "<%= config[:endpoint] %>/api/administrative-reports?permitnumber=63209-FP&year=2022&year=2021&year=2020"
```

```javascript
const axios require('axios');

const options = {
  method: 'GET',
  url: '<%= config[:endpoint] %>/api/administrative-reports',
  params: {
    permitNumber: '63209-FP',
    year: ['2022', '2021', '2020']
  }
}

axios
  .request(options)
  .then((response) => {
    console.log(response.data);
  })
  .catch((error) => {
    console.error(error);
  });
```

> Successful deletion will return the deleted records in JSON:

```json
[
  {
    "permitNumber": "FP-63209",
    "year": 2022
    // ...etc
  },
  {
    "permitNumber": "FP-63209",
    "year": 2021
    // ...etc
  },
  {
    "permitNumber": "FP-63209",
    "year": 2020
    // ...etc
  }
]
```

This endpoint retrieves administrative reports associated with a permit number.

This data corresponds to the DBB-013 Administrative Reporting form.

### HTTP Request

`GET /api/administrative-reports`

### Query Parameters

| Parameter    | Type   | Restrictions | Description                                                        |
| ------------ | ------ | ------------ | ------------------------------------------------------------------ |
| permitNumber | string | requried     | A string matching the well permit number.                          |
| year         | string | optional     | Define a year to retrieve the administrative report for that year. |
