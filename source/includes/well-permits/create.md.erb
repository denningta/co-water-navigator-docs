## Create Well Permits

```shell
curl -X POST "https://cowaternavigator/api/well-permits" \
  -d {
      "date": "2021-01",
      "permitNumber": "FP-63209"
    }

```

```javascript
const kittn = require("kittn");

let api = kittn.authorize("meowmeowmeow");
let kittens = api.kittens.get();
```

> The above command returns JSON structured like this:

```json
{
  "date": "2021-01",
  "permitNumber": "FP-63209"
  // ...etc
}
```

This endpoint creates a new permit number.

The record to be created should be send in the body of the request.

<aside class="notice">
  If a record already exists that matches the permitNumber - a "400 BadRequest" error will be returned.
</aside>

### HTTP Request

`POST /api/meter-readings/{permitNumber}/{date}`

### URL Parameters

| Parameter    | Required | Description                               |
| ------------ | -------- | ----------------------------------------- |
| permitNumber | true     | A string matching the well permit number. |
